DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": "django_cache_helper_test.db",
    }
}

# Make this unique, and don't share it with anybody.
SECRET_KEY = "12345"

MIDDLEWARE_CLASSES = ()

INSTALLED_APPS = ()
